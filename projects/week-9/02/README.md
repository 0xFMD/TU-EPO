# Week 9 - VM - IO Devices

## Compilation & Run

```bash
gcc -g ./zvm.c ./zvm_exception.c ./zvm_instruction.c ./zvm_io.c -o zvm

./zvm
```

## Output
The program will run a simple virtual machine that simulates IO devices. It will perform input and output operations, showing how the virtual machine handles IO requests.
