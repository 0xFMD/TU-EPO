# Week 10 - Memory Management API

## Compilation & Run

```bash
gcc -g ./zvm_block.c -o main

./main
```

## Output

The program runs a simple memory block API. It allocates a block, writes
reads bytes, clones and compares blocks, and resizes
the buffer.
